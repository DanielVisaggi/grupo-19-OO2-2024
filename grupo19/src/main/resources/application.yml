spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grupo19s
    username: root
    password: '123'
    #url: ${DB_URL}
    #username: ${USERNAME}
    #password: ${PASSWORD}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false